# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Use the GitHub Pages gem (includes Jekyll and Minima)
gem "github-pages", group: :jekyll_plugins
